<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit user</title>

        <!-- Latest compiled and minified CSS -->
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />

        <!-- Latest compiled JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <div class="container">
            <div class="row col-6">
                <form action="/put-crud" method="POST" class="row">
                    <input type="text" name="id" value="<%= data.id %>" hidden />
                    <div class="col-6 mb-3">
                        <label for="firstName" class="form-label"
                            >FirstName</label
                        >
                        <input
                            type="text"
                            class="form-control"
                            name="firstName"
                            aria-describedby="emailHelp"
                            value="<%= data.firstName %>"
                        />
                    </div>
                    <div class="mb-3 col-6">
                        <label for="lastName" class="form-label"
                            >LastName</label
                        >
                        <input
                            type="text"
                            class="form-control"
                            name="lastName"
                            value="<%= data.lastName %>"
                        />
                    </div>
                    <div class="mb-3 col-12">
                        <label for="address" class="form-label">Address</label>
                        <input
                            type="text"
                            class="form-control"
                            name="address"
                            value="<%= data.address %>"
                        />
                    </div>
                    <div class="mb-3 col-6">
                        <label for="phoneNumber" class="form-label"
                            >Phone number</label
                        >
                        <input
                            type="text"
                            class="form-control"
                            name="phoneNumber"
                            value="<%= data.phoneNumber %>"
                        />
                    </div>
                    <% let maleChecked = data.gender === 1 ? 'checked' : '' %>
                    <% let femaleChecked = data.gender === 0 ? 'checked' : '' %>
                    <div class="mb-3 col-3">
                        <label for="gender" class="form-label">Giới tính</label>
                        <div style="display: flex">
                            <div style="display: flex" class="me-3">
                                <label for="gender" class="form-label"
                                    >Nam</label
                                >
                                <input
                                    type="radio"
                                    class=""
                                    name="gender"
                                    value="1"
                                    <%= maleChecked %>
                                />
                            </div>
                            <div style="display: flex">
                                <label for="gender" class="form-label"
                                    >Nữ</label
                                >
                                <input
                                    type="radio"
                                    class=""
                                    name="gender"
                                    value="0"
                                    <%= femaleChecked %>
                                />
                            </div>
                        </div>
                    </div>
                    <% let adminChecked = data.roleId === '1' ? 'selected' : '' %>
                    <% let doctorChecked = data.roleId === '2' ? 'selected' : '' %>
                    <% let patientChecked = data.roleId === '3' ? 'selected' : '' %>
                    <div class="mb-3 col-3">
                        <label for="roleId" class="form-label">Role</label>
                        <select name="roleId" id="" class="form-control">
                            <option value="1" <%= adminChecked %> >Admin</option>
                            <option value="2" <%= doctorChecked %> >Doctor</option>
                            <option value="3" <%= patientChecked %> >Patient</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">
                        Update
                    </button>
                </form>
            </div>
        </div>
    </body>
</html>
